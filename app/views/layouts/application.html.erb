<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Homework Tracker" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag "application" %>
  </head>

  <body>
      <%= render 'layouts/nav' %>
    <div class="container">

    <%= yield %>
    <div id="footer">
    <%= link_to "Sign out", signout_path, :method => :delete %>
  </div>
  </div>
  <script>$(document).foundation();</script> <!-- ## uberkludge -->
  </body>
</html>
